DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_RDS_HOST:5432/postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YOUR_PASSWORD
POSTGRES_DB=postgres
RDS_HOST=YOUR_RDS_HOST
SESSION_SECRET=change-me-to-a-long-random-string-in-production

# Copy to .env on the server (project root):
#   cp .env.example .env

# Ollama (local AI — optional)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
OLLAMA_EMBED_MODEL=llama3.2
# OLLAMA_API_KEY=
# OLLAMA_ALLOW_REMOTE=true

# SMTP (Gmail app password — never commit real credentials)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=support@lmnt.fit
SMTP_PASSWORD=
SENDER_MAIL=support@lmnt.fit
APP_URL=https://testing.lmnt.fit
#APP_URL_LOCAL=http://localhost:3000
